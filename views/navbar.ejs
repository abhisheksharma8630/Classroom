<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top px-2">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Classroom</a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/tests"
            >Create Test</a
          >
        </li>
      </ul>
      <form class="d-flex" role="search" method="get" action="/tests">
        <input
          class="form-control me-2"
          type="search"
          placeholder="Enter Code"
          aria-label="Search"
          name="code"
        />
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
      <% if(currUser){ %>
      <ul class="navbar" style="list-style: none">
        <li>
          <a href=""><%= currUser.displayName%></a>
          <div class="sub-options bg-body-tertiary">
            <a href="">Profile</a>
            <a href="/logout">Logout</a>
          </div>
        </li>
      </ul>
      <%}else{%>
      <a href="/login" class="btn btn-secondary ms-lg-2 mt-sm-2 mt-lg-0">Login</a>
      <a href="/signup" class="btn btn-primary ms-lg-2 mt-sm-2 mt-lg-0">Signup</a>
      <% } %>
    </div>
  </div>
</nav>
<script></script>
